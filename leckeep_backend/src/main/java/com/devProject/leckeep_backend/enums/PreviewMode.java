package com.devProject.leckeep_backend.enums;

public enum PreviewMode {
    INLINE_IMAGE, //image dialog
    INLINE_PDF,  //PDF viewer dialog
    DOWNLOAD_ONLY, //download/open externally
    PAGE_RENDERED //paginated preview later
}
